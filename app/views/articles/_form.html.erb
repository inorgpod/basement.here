<%= form_for :article, url: articles_path  do |f|%>
    <p>
      <%= f.label :title %>
      <%= f.text_field :title %>
    </p>
    

    <p>
      <%= f.label :body %>
      <%# <%= f.text_area :body %> %>
       <strong>Body:</strong>
      <%= markdown(@article.body) %>
    </p>

    <p>
      <%= f.submit %>
    </p>
  
  <%end%>